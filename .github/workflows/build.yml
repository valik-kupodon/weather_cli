name: Build and Test

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust-version: [stable]

    steps:
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust-version }}
          override-arch: true

      - name: Check out code
        uses: actions/checkout@v2

      - name: Build Debug
        run: cargo build --verbose

      - name: Build Release
        run: cargo build --verbose --release

      - name: Run Tests
        run: cargo test --verbose

      - name: Clippy Check
        run: cargo install --force clippy && cargo clippy --verbose -- -D warnings

      - name: Rustfmt Check
        run: cargo install --force rustfmt && cargo fmt --verbose -- --check